name: fpt_server_shared
description: >
  Wire-contract types shared between the fpt_server backend and its
  fpt_server_website dashboard — one compiled definition instead of two
  hand-copied ones.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
